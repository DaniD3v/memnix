
move stuff from Mir_ -> generic_lang
remove canonical_idx
make lambdas at different nesting_depths equivalent
`Option<Color>` -> `Color`

crate splitting (remove clap dependency)
move ArenaBackedGraph to arena
re-expose graph debug output (AsDot is dead code since the out.dot dump was dropped from main.rs)
