
move stuff from Mir_ -> generic_lang
remove canonical_idx
ColorableExpr -> make a final version where the option is removed
generativity for arena
make lambdas at different nesting_depths equivalent
length tag for strings / ints so that they can't spoof other types
`Option<Color>` -> `Color`

crate splitting
move ArenaBackedGraph to arena
